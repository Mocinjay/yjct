// Base configuration for the Clypso target, Release.
// See Debug.xcconfig for why this file exists.

#include "../Pods/Target Support Files/Pods-Clypso/Pods-Clypso.release.xcconfig"

// Per-developer signing. Untracked — see Signing.xcconfig.example.
#include? "Signing.xcconfig"

// See Debug.xcconfig. Must match, or Release grows a warning Debug does not.
LM_SKIP_METADATA_EXTRACTION = YES
